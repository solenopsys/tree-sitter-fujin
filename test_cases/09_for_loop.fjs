// Test 9: For loop inside actor
type SumMsg = { arr: i32[] };
type SumOut = { type: "sum", total: i32 };

actor sum(msg: SumMsg) {
  let total: i32 = 0;
  for (let i: i32 = 0; i < msg.arr.length; i++) {
    total += msg.arr[i];
  }
  const out: SumOut = { type: "sum", total };
  emit out;
}
