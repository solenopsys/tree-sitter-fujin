// Union and union-in-field cases
type Status = "pending" | "running" | "done";
type Value = i32 | string | null;

type Task = {
  id: u64
  status: "pending" | "processing" | "done" | "failed"
};

type State = "cold" | "warm" | "hot";
