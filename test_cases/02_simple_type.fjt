// Types: basic structs, optional fields, no-semicolon tolerance
type Point = {
  x: i32
  y: i32
}

type User = {
  id: u64
  name: string
  email?: string
};

// Type without trailing semicolon
type Task = {
  id: u64
  status: "pending" | "done"
}
